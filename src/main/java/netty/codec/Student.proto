syntax = "proto3";  //版本
option java_outer_classname = "StudentPOJO";  //生成的外部类名

message Student{  //会在StudengPOJO外部类生成一个内部类Student，是真正发送的对象
  int32 id = 1; //Student类中有一个属性，名字为id，类型为int32(对应java的int),1表示属性序号,不是值
  string name = 2;
  
}

